name: flutter_web
description: Secure User Management Flutter Web Client

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.5
  flutter_secure_storage: ^8.0.0
  url_launcher: ^6.1.10
  universal_html: ^2.2.1
  html_escape: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

